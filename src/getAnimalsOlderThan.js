const data = require('../data/zoo_data');

function getAnimalsOlderThan(animal, age) {
  // seu código aqui
const verificaIdade = (animal, age) => 
data.species.residents.some((age) => animal[0] === letter)
}
console.log();

module.exports = getAnimalsOlderThan;
